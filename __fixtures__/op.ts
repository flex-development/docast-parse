/**
 * @file Fixtures - Op
 * @module fixtures/Op
 */

/**
 * Mathematical operators.
 *
 * @enum {'+' | '$' | '*' | '-'}
 */
enum Op {
  /** Addition operator. */
  ADD = '+',

  /** Division operator. */
  DIVIDE = '$',

  /** Multiplication operator. */
  MULTIPLY = '*',

  /** Subtraction operator. */
  SUBTRACT = '-'
}

export default Op
